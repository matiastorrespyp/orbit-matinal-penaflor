# Bitácora — Sesión 2026-07-06

Detalle fila por fila en `CHANGELOG_AI.md`.
Temas de la sesión: **(1) 11 Titulares — match por código de artículo; (2) Incentivo FARO — bimestre julio-agosto; (3) Incentivo FARO — leído 100% de la hoja; (4) Stock sin Venta — diagnóstico "no aparece en Render" (caché); (5) Stock sin Venta — exportación a Excel; (6) 11T por superficie (AS+Almacén+Kiosco) + tile CCC empresa real vs objetivo.**

---

# 1ª tarea — 11 Titulares: match SKU→marca por Código de Artículo

## 1. Pedido
Para la medición de 11 Titulares, usar las reglas con los **códigos de producto** del archivo `01_INPUTS/11 titulares autoservicio/11_titulares_autoservicios_match_codigos.xlsx` (contrato de datos preparado por el usuario).

## 2. Qué mide / decisiones
- La medición **sigue siendo por marca**: todas las variedades (todos los códigos) de una marca suman a esa marca (confirmado por el usuario).
- Las 11 líneas de la matriz coinciden 1:1 con los 11 titulares actuales. Normalización única: `SMF ICE`→`SMIRNOFF ICE`.
- **Opción A** (elegida por el usuario): el código es el match **primario**; si un SKU no está en la matriz, cae al match por **texto de `Marca`** → no se pierde ninguna variedad.
- Match: `ventas.Codigo` (SKU) == `codigo_articulo` de la hoja `DETALLE_SKU_11T_AS` (82 SKUs).

## 3. Cambios
- **`server_orbit.py`:** helpers `_codigos_11t_map()` (cacheado por mtime; `{}` si falta el archivo → cae al texto previo) y `_marca_11t_por_codigo(df)`. Asignación en 2 pasos (código primario → texto fallback) en las 4 rutas vivas: `gerencia_once_titulares`, `once_titulares_zona`, snapshot 11T de `gerencia_cierre_mes` y `_cierre_once_titulares`.
- **`tools/generar_cierre_mensual.py`:** `_marca_11t()` reescrito a código-primario + fallback texto.
- **NO se tocó:** OP/Vinotecas, cobertura, ni los drill-down `11t_empresa`/`11t_vendedor` (leen el legacy `mod_11_titulares.csv`, otra lineage).

## 4. Validación
- `py_compile` OK. Endpoints 200, 82 códigos cargados.
- Delta sobre `ventas_acumulada.csv`: **0 filas cambian de marca** (el código coincide con el texto donde ya resolvía; el texto cubre el resto) → sin regresión, con precisión agregada.
- `_marca_11t` del cierre probado: código con texto errado→corrige; variedad fuera de matriz→suma por texto (Opción A); código de cerveza→ANTARES.

## 5. Deploy
- La matriz `...match_codigos.xlsx` **viaja al commit** (orden explícita del usuario) para que Render aplique el match por código; sin ella cae al match por texto (no rompe).

---

# 2ª tarea — Incentivo FARO: bimestre julio-agosto (nuevas categorías por código de SKU)

## 1. Síntoma
El usuario editó `incentivo_club_faro .xlsx` para el nuevo bimestre pero la pantalla de FARO no cambiaba.

## 2. Causa raíz
La lógica de QUÉ se mide estaba **hardcodeada** para mayo-junio (Alaris+FLM / Antares / Familia Smirnoff, por texto) y el período fijo en meses [5,6]. Del xlsx solo se leían objetivos y premios. Como `ventas_acumulada.csv` hoy solo tiene julio, el filtro [5,6] además descartaba todo.

## 3. Definición nueva (julio-agosto, match por CÓDIGO de SKU)
| Categoría | Canal | Umbral | SKUs | Premio |
|---|---|---|---|---|
| Smirnoff Ice | Tradicional (kiosco+almacén) | 3 bot/SKU | 35103/35104/35105 | 2000 |
| Vinos Red Blends | Autoservicio | 6 bot/SKU | 80089/74684/44395/71716/74735/42376/74737 | 1000 |
| Familia Gordons (x700) | Autoservicio | 6 bot/SKU (tope 3/cli) | 30139/30075/30134 | 1000 |

- **Conteo:** cada SKU con ≥ umbral botellas suma 1 cobertura; se suman por cliente.
- **Regla de conteo confirmada** por el usuario: umbral por SKU; Smirnoff Ice se vende en pack de 6 → siempre cubre el mínimo 3.

## 4. Cambios
- **`server_orbit.py`:** constantes FARO reescritas; objetivos por columnas en orden de categorías; premios reconocen gordons/red blends/smirnoff ice; ventas filtradas al bimestre y categoría por código; conteo por SKU con umbral y tope. Endpoints exponen `categorias_orden`+`categorias_meta`; período dinámico.
- **`portal.html`:** gerencia y vendedor iteran `categorias_orden`/`categorias_meta` (ya no 3 claves fijas); textos de regla/período desde el payload.
- **`01_INPUTS/incentivo_club_faro .xlsx`:** commiteado para que Render tome objetivos/premios.

## 5. Validación
- `py_compile` + `node --check` OK; endpoints 200; objetivos leídos = xlsx (V3 60/0/0, V4-V10 38/13-15/8; Esteban 212/56/32, Raul 76/28/16).
- **Playwright** login gerencia + vendedor V8: 3 categorías nuevas, umbrales, julio-agosto, drill-down por SKU, **0 errores de consola**. Logrado bajo (solo 4 días de julio cargados) — esperado.

---

# 3ª tarea — Incentivo FARO: toda la definición se lee de la hoja (data-driven)

## 1. Pedido
Que FARO trabaje **leyendo la hoja**, para que cada bimestre el usuario edite **solo el Excel** (así creía que ya funcionaba; por eso solo modificó la hoja).

## 2. Cambio
- **`server_orbit.py`:** nuevo **`_faro_config()`** (cacheado por mtime) que parsea TODA la definición de `incentivo_club_faro*.xlsx` (Hoja1) tal como el usuario la escribe: categorías (fila encabezado con "vendedores"), canal (banda superior con forward-fill), objetivos (filas con vendedor numérico), y de las **reglas en texto libre** → códigos de SKU (números ≥4 dígitos por línea de categoría), umbral (nº antes de "botellas/latas" o default 3 trad/6 AS), tope ("N máximo"), premios (fila MILLAS por tokens del nombre), período/meses (título) y supervisores (línea "Esteban… Raúl…").
- Se **eliminaron todas las constantes hardcodeadas**; `_faro_ventas(cfg)` y `_faro_detalle_vendedor(df,cod,cfg)` reciben la config; los endpoints la construyen. Si la hoja no se puede leer → 200 con `error` (no datos falsos). Caché de ventas contempla el mtime del xlsx.
- **Frontend:** sin cambios (ya era data-driven).

## 3. Validación
- `_faro_config()` reproduce **EXACTO** lo que estaba hardcodeado (cats, seg TRAD/AS/AS, umbral 3/6/6, cap gordons=3, SKUs, objetivos, premios, meses (7,8), supervisores). Endpoints con números idénticos.
- **Playwright** gerencia + V8: render correcto, **0 errores de consola**.

## 4. Cómo debe escribirse la hoja (para que el parser lea bien)
Requisitos de layout en `NEXT_TASK.md` (fila "vendedores" + nombres de categoría, banda de canal arriba, grilla de objetivos con código de vendedor, reglas en texto con códigos de SKU + mínimo de botellas + tope, fila PREMIOS, título con meses, línea de supervisores). Si cambia mucho el formato y algo no se lee, el endpoint avisa con `error`.

---

# 4ª tarea — Stock sin Venta: "hice la pantalla pero no la veo en gerencia"

## 1. Síntoma
El usuario había hecho la pantalla **Stock sin Venta** (commit `994efea`, sesión previa) pero no la veía en gerencia.

## 2. Diagnóstico (sin tocar código)
- El ítem de menú es **HTML estático** en `portal.html:1059` (sección Productos, bajo "Incentivo Dada"), **sin filtro de rol** → se muestra a toda gerencia. Router, fetch y render presentes.
- `git`: los commits `994efea` (pantalla) y `9402d45` (cierre del día) ya estaban en `origin/master`; `stock.xlsx` (102 KB, no inflado) trackeado. El endpoint carga el xlsx **lazy** dentro de la ruta, no al arranque → no puede tumbar el boot.
- El usuario trabaja **solo en Render**, no en local (no había nada en :8502).
- **Prueba discriminante:** los datos del cierre de hoy **sí** se veían en Render, pero el botón **no** → Render servía código nuevo, el navegador servía `portal.html` viejo cacheado.

## 3. Causa raíz
**Caché del navegador.** No era código ni deploy.

## 4. Solución
**Hard refresh** (`Ctrl+Shift+R`). Confirmado por el usuario: "ahora sí aparece". → [[05_ERRORES_Y_SOLUCIONES]]: ante "no aparece en Render" con datos frescos visibles, sospechar primero caché del navegador, no el deploy.

---

# 5ª tarea — Stock sin Venta: exportación a Excel (.xlsx)

## 1. Pedido
Agregar un botón de exportación a Excel del reporte, **solo con los productos con stock y sin ventas** (que es exactamente lo que ya lista la pantalla).

## 2. Cambios
- **`server_orbit.py`:** cálculo extraído de la ruta JSON a un **helper compartido `_stock_sin_venta_payload()`** (mismo criterio, sin divergencia); la ruta `GET /api/gerencia/stock_sin_venta` lo consume. Nuevo endpoint **`GET /api/gerencia/stock_sin_venta/export`** que genera el `.xlsx` server-side (pandas + openpyxl, **mismo patrón que `alertas_caida/export`**): hoja "Stock sin venta" con Código, Producto, Disponible, Reserva, En tránsito, Bultos total; anchos auto; nombre `stock_sin_venta_<mes>.xlsx`; 404 si falta el xlsx (sin datos falsos).
- **`portal.html`:** botón "⬇ Exportar Excel" (`window.location.href` al endpoint) junto al CSV existente; función `descargarStockSinVentaExcel()`. CSV intacto.

## 3. Validación (server real :8502)
- `/api/gerencia/stock_sin_venta` → HTTP 200, **115 productos, 16.103 u**.
- `/export` → HTTP 200, `Content-Type` xlsx, `Content-Disposition: stock_sin_venta_julio_2026.xlsx`.
- Archivo bajado: **"Microsoft Excel 2007+"** válido → 115 filas (= total_sin_venta), suma Disponible 16.103 (= unidades_sin_venta), **todas Disponible > 0**. Concilia con el JSON.

## 4. Deploy
Commiteado y pusheado a `master` (dispara deploy Render). El usuario confirmó: "ahora está bien".

---

# 6ª tarea — 11T por superficie (AS+Almacén+Kiosco) + tile CCC empresa real vs objetivo

## 1. Pedido
1. La medición de **11 Titulares** no debe ser "solo autoservicio" sino **Autoservicio + Almacén + Kiosco** — que toda la medición use ese criterio.
2. Nuevo archivo `01_INPUTS/objccc.xlsx` para llevar en el **dashboard** una estadística **CCC total empresa: real vs objetivo**.
3. Ya cargó los objetivos del mes en `01_INPUTS/objetivo 11T.xlsx`.

## 2. Diagnóstico
- El 11T (`gerencia_once_titulares` y gemelas) contaba CCC por marca titular **sin ningún filtro de superficie** → sumaba On Premise, Vinotecas, Away From Home, etc. (más amplio de lo que la empresa mide).
- El endpoint `gerencia_ccc_empresa` ya existía (lo llamaba `portal.html` como `D.cccEmpresa`) pero devolvía 3 buckets por vendedor sin objetivo. `objccc.xlsx` trae objetivo CCC por **5 canales** (On Premise 30 / Vinotecas 15 / On Premise Noche 11 / Autoservicios 145 / Tradicionales 845 = **1046**).

## 3. Decisiones (confirmadas con el usuario)
- **Superficie 11T** = Autoservicio + Almacén + Kiosco. "AS puro" (Ramo `AUTOSERVICIO`, NO Cash&Carry ni Mayoristas) **pero SÍ** cuenta el Subramo **"Autoservicio Tradicional"** (autoservicios chicos bajo Ramo `TRADITIONAL TRADE`) — 2ª pregunta confirmada. Ver [[REGLAS_NEGOCIO_PAV]].
- **Fuente del tile CCC empresa** = **mes vivo** (`ventas.csv`), no trimestre.
- **Canales objccc** clasificados por **Ramo** (así se armó el objetivo): ahí el Subramo "Autoservicio Tradicional" cuenta como **Tradicional** (distinto del 11T, que lo cuenta como AS). Dos métricas, dos definiciones — documentado.

## 4. Cambios
- **`server_orbit.py`:**
  - Nuevo helper **`_mask_superficie_11t(df)`** (fail-open si falta Ramo/Subramo). Aplicado en las **4 mediciones vivas**: `gerencia_once_titulares`, `once_titulares_zona`, bloque 11T de `gerencia_cierre_mes`, `_cierre_once_titulares`.
  - **`gerencia_ccc_empresa` reescrito**: CCC mes vivo por 5 canales (helper `_canal_ccc_empresa` por Ramo) vs objetivo (`_objetivos_ccc_empresa` ← `objccc.xlsx`). Devuelve `canales[]` + `empresa{total,objetivo_total,pct, trad/as/op compat}`. Todo nativo (int/round) → jsonify OK en Render.
- **`portal.html`:** kcard "CCC Compradores Mes" → `real / objetivo · %`; nueva card gerencia **"📊 CCC Empresa · real vs objetivo"** con tabla por canal.
- **NO se tocó:** `tools/generar_cierre_mensual.py::_11t_por_vend` (ranking mensual por cobertura de botellas, otra metodología, corre por .bat) → anotado en `NEXT_TASK.md`.

## 5. Validación (test_client + Render en vivo)
- Filtro efectivo: titulares en superficie pasan de 179→72 filas; se excluyen On Premise/Vinotecas/Cash&Carry/Away/Restaurant; se conservan AS+Almacén+Kiosco+Autoserv.Tradicional. Alma Mora CCC 15, Dada 16.
- `ccc_empresa`: total **44 / 1046 = 4,2%**; canales reconcilian (37+2+2+2+1=44). % bajos = esperados (mes recién arranca).
- **Render `orbit-matinal-penaflor.onrender.com`**: `/api/gerencia/once_titulares` y `/api/gerencia/ccc_empresa` → HTTP 200 con **números idénticos a local** tras el push. Deploy confirmado en vivo.

## 6. Bug preexistente detectado (fuera de alcance)
`GET /api/gerencia/cierre_mes` → **500**: `01_INPUTS/ventas_mes.csv` está delimitado por `;` pero `_leer_ventas_mes_csv` lo lee con `sep=','` (ParserError en el sell-out del cierre, **antes** del bloque 11T). Probado: **HEAD (git) en el directorio real también da 500** → no lo introdujo este cambio. Anotado en `NEXT_TASK.md`. → [[05_ERRORES_Y_SOLUCIONES]].

---

## Commits de la sesión

| Commit | Qué |
|---|---|
| `add13d0` | feat(11T): match por Código Art. exacto (matriz oficial) como fuente primaria |
| `4ca7fc2` | data(11T): matriz oficial de códigos de 11 Titulares AS (para Render) |
| `25a778a` | feat(Incentivo FARO): bimestre julio-agosto con categorías por código de SKU |
| `632ee99` | refactor(Incentivo FARO): toda la definición se lee de la hoja (sin hardcode) |
| `690c286` | feat(Stock sin Venta): exportación a Excel (.xlsx) del reporte |
| `265c6ba` | feat(11T + CCC): 11T mide solo AS+Almacén+Kiosco; tile CCC empresa real vs objetivo |
