# Bitácora — Sesión 2026-07-03

Detalle fila por fila en `CHANGELOG_AI.md`.
Tema de la sesión: **Nuevo botón "Incentivo Dada" en gerencia — cobertura de Dada Tinto Verano en autoservicios, con detalle de pedidos**.

---

## 1. Pedido

Agregar en el perfil de **gerencia** un botón **debajo de Plan Frizze** llamado **Incentivo Dada**. Adentro: **arriba la imagen** cargada en `01_INPUTS/dadatinto.png` y **debajo una tarjeta con el seguimiento de la cobertura del producto**. El objetivo de la distribuidora está en `01_INPUTS/DADAVERANOOBJ.xlsx` y las ventas para sacar las coberturas en `01_INPUTS/dadatinto.csv`.

**2º pedido (mismo tema):** que al **clickear la cantidad de clientes con compra** se abra una tarjeta con el **detalle de los clientes con compra**, con la **cantidad y la fecha del pedido**.

## 2. Qué mide (fuente: `01_INPUTS/DADAVERANOOBJ.xlsx`)

- Regla textual del xlsx: *"se mide ccc (cobertura del producto 74884 Dada tinto de verano) en autoservicios objetivo 38 clientes."*
- **Objetivo distribuidora: 38 clientes** de autoservicio con cobertura del producto **74884 (DADA LATA TINTO VERANO 4X6X355)**.
- **Cliente cubierto** = superficie **autoservicio** + **compra válida** (`ImporteNeto>0`) + **≥6 botellas** del código objetivo (regla Cobertura Autoservicio de CLAUDE.md). **Excluye V1/V2/V5/V20.**
- El objetivo (38) y el código (74884) se **parsean del texto** del xlsx con regex → fuente única y editable, sin hardcodear.

## 3. Fuentes reales de cada dato (sin mock, sin inventar)

- **Objetivo + código** ← `DADAVERANOOBJ.xlsx` (texto libre parseado).
- **Ventas / cobertura** ← `dadatinto.csv` (ya viene filtrado al producto 74884, 58 filas, mismo layout que `ventas.csv`; sep `;`, latin-1). Autoservicio = `Ramo`/`Subramo` contiene "autoservicio".
- **Pedidos por cliente** (cantidad + fecha) ← líneas del cliente agrupadas por **`FechaComprobante`** (regla de facturación), botellas = `CantBase`.
- **Imagen** ← `dadatinto.png` copiada a la carpeta del frontend y servida en `/dadatinto.png` (patrón Frizze).

## 4. Cambios

- **`server_orbit.py`:**
  - `_incentivo_dada_objetivo()` — parsea objetivo y código del xlsx (fallback a 38 / 74884).
  - `_incentivo_dada()` — lee `dadatinto.csv`, clasifica autoservicio, agrupa por cliente y arma `logrado/objetivo/faltan/avance` + **por vendedor** + **detalle de clientes** con `pedidos:[{fecha,botellas}]`. `jsonify(_to_native(...))` → Render-safe.
  - Endpoint nuevo `GET /api/gerencia/incentivo_dada`. **Aditivo:** no toca Frizze, FARO, Planes AS ni el pipeline de datos.
- **`PAV MATINAL PE_A FLOR/portal.html`:**
  - Item de menú **🍷 Incentivo Dada** bajo Plan Frizze, carga en `loadRole` (gerencia), router `gRender`, título y render `gIncentivoDada` (hero con la imagen + tarjeta: KPIs, barra de avance, por vendedor, tabla de clientes).
  - KPI **"Clientes con compra"** clickeable → modal `ddVerClientes()` con, por cliente, nombre/localidad/vendedor, total de botellas y **un chip por pedido (`📅 fecha · N bot.`)**. CSS `.dd-*`.
- **Imagen** `dadatinto.png` en la carpeta del frontend (servida por la ruta estática existente).

## 5. Validación (visible, no solo "200")

- Endpoint por import directo, en vivo local (8502) **y en producción**: **objetivo=38, logrado=22, faltan=16, avance=57.9%** (V8=14, V10=6, V4=2, 22 clientes).
- **Cuadre de pedidos:** suma de pedidos == total por cliente en los 22 (0 descuadres). Ej. **YANDEN S.A.S 12 bot → 17/06 (6) + 30/06 (6)**.
- `/dadatinto.png` → 200 (2.26 MB) local y en Render.
- `py_compile` OK; server reiniciado en :8502 tras cada cambio.
- **En vivo en `https://orbit-matinal-penaflor.onrender.com`:** `/api/gerencia/incentivo_dada` → 200 con `pedidos`, imagen 200.

## 6. Detalles / decisiones

- **"Cubierto" = autoservicio + ≥6 botellas.** Hoy da lo mismo que CCC puro (todos los autoservicio con compra ya tienen ≥6), pero se aplica el umbral de la regla de cobertura AS por correctitud a futuro.
- **V20 y V2/V5 excluidos** del conteo con objetivo (regla del proyecto). En este dataset V20 ni aparece en autoservicio.
- **Deploy:** `dadatinto.csv` y `DADAVERANOOBJ.xlsx` **viajan al commit** porque el endpoint los lee desde `01_INPUTS/` también en Render (mismo patrón que `ventas.csv`/`resultado.xlsx`, que ya están trackeados). Sin ellos, el endpoint daría 404 en producción. **No** se commiteó `01_INPUTS/dadatinto.png` (redundante con la copia del frontend).

## 7. Pendiente / a tener en cuenta

- **Actualización mensual:** cuando gerencia suba ventas/objetivo nuevos, hay que **volver a commitear** `dadatinto.csv` y `DADAVERANOOBJ.xlsx` (o sumarlos al flujo de cierre) para que Render refleje los números.
- **Sin vista de vendedor:** el pedido fue solo gerencia. Si se quiere que cada vendedor vea sus clientes Dada, replicar el patrón Frizze (endpoint `/api/vendedor/<vid>/incentivo_dada` + tab).
- Confirmar con gerencia la definición de "cubierto" si en el futuro quieren CCC puro o incluir otras superficies.

---

# 2ª tarea — Acciones Comerciales: tarjetas de acciones ON (On Premise / VTK / TDB / Catering)

## 1. Pedido

Agregar a la **pantalla de Acciones Comerciales** las tarjetas necesarias para las **acciones de ON** que están en `01_INPUTS/ACCIONES COMERCIALES/2026-07/acciones_comerciales_julio_2026_orbit_penaflorON.xlsx`.

## 2. Qué son (fuente: el xlsx ON)

- Hoja `01_Acciones`: **6 combos de incorporación sin cargo** (mecánica, canales, compra requerida, beneficio, tope). Ej. "comprando 1 caja de cualquier 11T, se entregan 2 cajas sin cargo del mismo 11T".
- Hoja `02_Detalle_Productos`: **468 líneas** de productos elegibles por **subcanal** (ON DIA, ON NOCHE, VINOTECAS, TIENDA DE BEBIDAS, CATERING) y **línea comercial**.
- Las 6: **ON-11T** (96 prod), **VTK-TDB-11T** (166), **ON-INNOV** (88), **VTK-TDB-INNOV** (88), **ANTARES** (6), **DADA-TDV** (24).

## 3. Decisión de diseño

Son **informativas** (combos "compra 1 → llevás N sin cargo"), NO tienen footprint de inversión como las acciones AS/Trad (que calculan $ y clientes desde ventas.csv). Se muestran como **catálogo aparte** (`acciones_on`), con el **motor de inversión intacto**. No se fuerza $0/clientes falsos.

## 4. Cambios

- **`server_orbit.py`:**
  - `_acc_on_file()` — autodetecta el `*ON.xlsx` del mes (sufijo ON antes de la extensión).
  - `_acc_on_cards()` — parsea el xlsx (cacheado por mtime) → 6 tarjetas con `grupos:[{subcanal, lineas:[{lc, productos:[{codigo,descripcion,estado}]}]}]`. Limpia `\xa0`.
  - Su mtime se sumó a **`_acc_mes_sig`** (al resubir el archivo, el payload se refresca solo) y se expone como **`acciones_on`** en el payload de `acciones_mes` (gerencia y vendedor).
- **`PAV MATINAL PE_A FLOR/portal.html`:**
  - Sección **"🌙 Acciones ON"** al pie de la pantalla (gerencia `gAccionesComerciales` + vendedor `vAcciones`), helper compartido `accOnSectionHTML(scope,dat)`.
  - Tarjeta: id, "Sin cargo", título, chips de canal, mecánica, combo `📦 compra → 🎁 beneficio`, tope, "Ver productos (N)". Click → modal `accShowOn` (reusa `.accd-*`) con productos por subcanal → LC; **no vigentes resaltados en ámbar** con su estado. CSS `.accon-*`.

## 5. Validación (visible, no solo "200")

- Endpoints: `/api/gerencia/acciones_mes` → **23 acciones + 6 `acciones_on`**; `/api/vendedor/V8/acciones_mes` también las 6.
- `node --check` del JS del portal OK.
- **Playwright con login gerencia:** sección ON con 6 tarjetas, modal abre con grupos por subcanal (ON DIA · 61 productos, etc.), **0 errores de consola**. Screenshots revisadas (tarjetas + modal con productos, incluidos los "En proceso de alta" / "SIN ESTADO EN MAESTRO" en ámbar).
- **En vivo en Render:** confirmado `acciones_on=6` con los conteos de productos correctos.

## 6. Detalles / decisiones

- **Deploy:** el input `...penaflorON.xlsx` **viaja al commit** (mismo criterio que los otros archivos de acciones); sin él, la sección ON sale vacía en Render.
- **Convención mensual:** subir `acciones_comerciales_<mes>_..._penaflorON.xlsx` en `01_INPUTS/ACCIONES COMERCIALES/<YYYY-MM>/`. El sistema autodetecta el `*ON.xlsx` del mes; sin tocar código.
- **Sin mapeo de vendedor:** las ON aplican por canal (ON/VTK/TDB/Catering), no traen vendedor → hoy se muestran a todos. Si gerencia quiere filtrar por vendedor de On Premise, definir la regla.

---

## Commits de la sesión

| Commit | Qué |
|---|---|
| `dcd3a94` | feat(incentivo dada): botón gerencia + cobertura Dada Tinto Verano con detalle de pedidos — pusheado y verificado en Render |
| `257b23d` | docs(obsidian): bitácora 2026-07-03 — Incentivo Dada |
| `0569a1a` | feat(acciones comerciales): tarjetas de acciones ON (On Premise/VTK/TDB/Catering) — pusheado y verificado en Render |
