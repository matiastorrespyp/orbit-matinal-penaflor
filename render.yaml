###############################################################
# ORBIT PAV Matinal · Peñaflor — Render deployment config
# https://render.com/docs/yaml-spec
###############################################################

services:
  - type: web
    name: orbit-matinal-penaflor
    runtime: python
    plan: free              # tier gratuito de Render

    # Build
    buildCommand: pip install -r requirements.txt

    # Start — gunicorn sirve la app Flask
    # 1 worker = sin conflictos de escritura en SQLite
    startCommand: >
      gunicorn server_orbit:app
      --bind 0.0.0.0:$PORT
      --timeout 120
      --workers 1
      --worker-class sync

    # Environment variables
    envVars:
      - key: FLASK_DEBUG
        value: "false"
      - key: PYTHON_VERSION
        value: "3.11.9"

    # Health check — endpoint liviano, sin leer archivos de datos
    healthCheckPath: /api/healthz

    # Auto-deploy DESHABILITADO — deployar manualmente desde Render dashboard
    # para evitar que commits de datos/config borren orbit.db en produccion
    branch: master
    autoDeploy: false
